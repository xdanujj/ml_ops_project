name: Databricks CI/CD Pipeline

on:
  push:
    branches: [ main ]  # Runs automatically when code is pushed to 'main'

jobs:
  build-train-deploy:
    runs-on: ubuntu-latest

    steps:
      # Step 1️⃣: Checkout repository code
      - name: Checkout repository
        uses: actions/checkout@v4

      # Step 2️⃣: Set up Python environment
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      # Step 3️⃣: Install dependencies
      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install databricks-cli pytest

      # Step 4️⃣: Run tests (skipped for demo)
      - name: Run tests
        run: echo "⚙️ No tests available, skipping pytest step..."

      # Step 5️⃣: Trigger Databricks Training Job
      - name: Trigger Databricks Training Job
        env:
          DATABRICKS_HOST: ${{ secrets.DATABRICKS_HOST }}
          DATABRICKS_TOKEN: ${{ secrets.DATABRICKS_TOKEN }}
          DATABRICKS_JOB_ID: ${{ secrets.DATABRICKS_JOB_ID }}
        run: |
          echo "🚀 Checking Databricks CLI version..."
          databricks --version

          echo "🚀 Triggering Databricks job..."
          databricks jobs run-now --job-id $DATABRICKS_JOB_ID
          echo "✅ Databricks job triggered successfully!"

      # Step 6️⃣: Build and Push Docker Image
      - name: Build and Push Docker Image
        if: success()
        env:
          DOCKER_USER: ${{ secrets.DOCKER_USER }}
          DOCKER_PASS: ${{ secrets.DOCKER_PASS }}
        run: |
          echo "🐳 Building Docker image..."
          echo $DOCKER_PASS | docker login -u $DOCKER_USER --password-stdin
          docker build -t $DOCKER_USER/crop-rec-model:latest .
          docker push $DOCKER_USER/crop-rec-model:latest
          echo "✅ Docker image pushed successfully!"
